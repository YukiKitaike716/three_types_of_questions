name: three_types_of_questions
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  collection: ^1.17.1
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.3.6
  freezed_annotation: ^2.2.0
  gap: ^3.0.0
  json_annotation: ^4.8.1
  logger: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.4.2
  freezed: ^2.3.3
  json_serializable: ^6.6.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/
